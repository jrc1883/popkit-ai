# PopKit Deploy Plugin - Python Dependencies

# Shared utilities package (required)
popkit-shared>=0.1.0

# Note: Platform-specific CLIs must be installed separately:
# - Docker CLI (docker) for Docker deployments
# - Vercel CLI (vercel) for Vercel deployments
# - Netlify CLI (netlify) for Netlify deployments
# - Wrangler CLI (wrangler) for Cloudflare deployments
# - npm for npm registry publishing
# - twine for PyPI publishing
